import fsp from 'fs/promises';
import path from 'path';
import rimraf from 'rimraf';
import mkdirp from 'mkdirp';
import crypto from 'crypto';
import { dirRoot, dirTestDist } from './utils/dir';

const testDist = dirTestDist();

describe('OrderedFsCrawler', () => {
  // beforeAll(async () => {
  //   mkdirp(dirRoot('.disttest'))
  //   //
  // });

  // afterAll(async () => {
  //   //
  // });

  // it('should crawl all files', async () => {
  //   //
  // });
  it('todo', () => void expect(true).toBe(true));
});

